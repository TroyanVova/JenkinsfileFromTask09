pipeline {
    agent { label 'firstAgent' } // агент на VM2

    stages {
        stage('Build') {
            steps {
              sh 'scp -P 22 /var/log/apache2/access.log jenkins@192.168.100.5:/var/lib/jenkins/logs/vm2ApacheEroor.log'
            }
        }
        stage('Test') {
            steps {
              sh 'echo "Test stage"'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "Deploy stage"'
            }
        }
    }
}
